# Admin Module

Gathers portal statistics and provides administrative actions for dashboard oversight.

## Data & Request Flow

* **Incoming Requests**: Route handler requests targeting `/api/admin`.
* **Outgoing Requests**: Relational database queries to retrieve platform-wide metadata counts.
