# Subject Module

Manages high-level subject catalog directories (e.g. System Design).

## Data & Request Flow

* **Incoming Requests**: `/api/subjects` route handlers; topic modules verifying subject categories.
* **Outgoing Requests**: Database actions editing subject names and descriptions.
